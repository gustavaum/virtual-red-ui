# Virtual Red UI sandbox

Prototipos com `Jekyll`

##install jekyll
<https://jekyllrb.com/docs/installation/>

## Run

```bash
jekyll serve --livereload --host=0.0.0.0
```

Sass partials dento de `_sass`.

